<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>spring</title>
    <url>/2022/03/05/spring/</url>
    <content><![CDATA[<h1 id="IOC"><a href="#IOC" class="headerlink" title="IOC"></a>IOC</h1><h2 id="负责对象创建，对象和对象之间的调用"><a href="#负责对象创建，对象和对象之间的调用" class="headerlink" title="负责对象创建，对象和对象之间的调用"></a>负责对象创建，对象和对象之间的调用</h2><pre><code>&lt;!--创建一个对象,默认无参构造--&gt;
&lt;bean id=&quot;user&quot; class=&quot;com.company.User&quot;&gt;

    &lt;!--属性注入，有参构造--&gt;
    &lt;constructor-arg name=&quot;height&quot; value=&quot;12.3&quot;&gt;&lt;/constructor-arg&gt;
    &lt;constructor-arg name=&quot;width&quot; value=&quot;22.3&quot;&gt;&lt;/constructor-arg&gt;

    &lt;!--属性注入,set方法,类中必须含有属性的set方法--&gt;
    &lt;property name=&quot;name&quot; value=&quot;zhangSan&quot;&gt;&lt;/property&gt;
    &lt;property name=&quot;sex&quot; value=&quot;男&quot;&gt;&lt;/property&gt;

    &lt;!--注入字面量null--&gt;
    &lt;property name=&quot;nullTest&quot; &gt;
            &lt;null/&gt;
    &lt;/property&gt;
    &lt;!--注入带特殊符号 &lt; &gt; &#39; &quot; &amp;的值,用![CDATA[ 想填的值]]--&gt;
    &lt;property name=&quot;notNormal&quot; &gt;
        &lt;value&gt;
            &lt;![CDATA[&lt;hahahahah&gt;]]&gt;
        &lt;/value&gt;
    &lt;/property&gt;
    &lt;!--注入外部bean在对象中导入其他类的对象,ref=&quot;类id&quot;--&gt;

    &lt;!-- &lt;property name=&quot;dog&quot; ref=&quot;dog&quot;&gt;&lt;/property&gt; --&gt;
    &lt;!--被注入的外部bean--&gt;
    &lt;!--&lt;bean id=&quot;dog&quot; class=&quot;com.company.Dog&quot;&gt;&lt;/bean&gt;--&gt;
    &lt;!--注入内部bean,和上面注释外部bean注入作用一样--&gt;
    &lt;property name=&quot;dog&quot; &gt;
        &lt;bean id=&quot;dog&quot; class=&quot;com.company.Dog&quot;&gt;&lt;/bean&gt;
    &lt;/property&gt;

    &lt;!--级联赋值外部bean注入，然后给外部bean的属性,懒得写--&gt;

    &lt;!--注入数组,集合,映射--&gt;
    &lt;property name=&quot;arr&quot;&gt;
        &lt;array&gt;
            &lt;value&gt;
                aaa,bbb
            &lt;/value&gt;
        &lt;/array&gt;
    &lt;/property&gt;

    &lt;!--list存的是Dog对象,由外部bean导入--&gt;
    &lt;property name=&quot;list&quot;&gt;
        &lt;list&gt;
            &lt;ref bean=&quot;dog1&quot;&gt;&lt;/ref&gt;
            &lt;ref bean=&quot;dog2&quot;&gt;&lt;/ref&gt;
        &lt;/list&gt;
    &lt;/property&gt;

    &lt;property name=&quot;map&quot;&gt;
        &lt;map&gt;
            &lt;entry key=&quot;111&quot; value=&quot;aaa&quot;&gt;

            &lt;/entry&gt;
        &lt;/map&gt;

    &lt;/property&gt;
    &lt;property name=&quot;set&quot;&gt;
        &lt;set&gt;
            &lt;value&gt;
                qqq,www,eee
            &lt;/value&gt;
        &lt;/set&gt;
    &lt;/property&gt;


&lt;!--这是上边list存的外部bean--&gt;
&lt;/bean&gt;
&lt;bean id=&quot;dog1&quot; class=&quot;com.company.Dog&quot;&gt;
    &lt;property name=&quot;name&quot; value=&quot;dog1&quot;&gt;&lt;/property&gt;
&lt;/bean&gt;
&lt;bean id=&quot;dog2&quot; class=&quot;com.company.Dog&quot;&gt;
    &lt;property name=&quot;name&quot; value=&quot;dog2&quot;&gt;&lt;/property&gt;
&lt;/bean&gt;
</code></pre>
]]></content>
  </entry>
  <entry>
    <title>二叉树</title>
    <url>/2022/02/24/%E4%BA%8C%E5%8F%89%E6%A0%91/</url>
    <content><![CDATA[<h1 id="二叉树链式存储"><a href="#二叉树链式存储" class="headerlink" title="二叉树链式存储"></a>二叉树链式存储</h1><h2 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h2><span id="more"></span>
<pre><code>//节点类，包含数据，左右孩子指针
class TreeNode&#123;
    int data;
    TreeNode left;
    TreeNode right;
&#125;

//二叉树类中手动初始化一棵树
public void init()&#123;
    root = new TreeNode(1);
    root.left = new TreeNode(2);
    root.right = new TreeNode(3);
    root.left.left = new TreeNode(4);
    root.left.right = new TreeNode(5);
    root.right.left = new TreeNode(6);
    root.right.right = new TreeNode(7);
&#125;
</code></pre>
<h2 id="遍历"><a href="#遍历" class="headerlink" title="遍历"></a>遍历</h2><pre><code>//前序遍历，中左右顺序
public void frontShow()&#123;
    System.out.println(data);
    if(left!=null)&#123;
        left.frontShow();
    &#125;
    if(right!=null)&#123;
        right.frontShow();
    &#125;
&#125;

//中序遍历，左中右顺序
public void midShow()&#123;
    if(left!=null)&#123;
        left.midShow();
    &#125;
    System.out.println(data);
    if(right!=null)&#123;
        right.midShow();
    &#125;
&#125;

//后序遍历，左右中顺序
public void afterShow()&#123;
    if(left!=null)&#123;
        left.afterShow();
    &#125;
    if(right!=null)&#123;
        right.afterShow();
    &#125;
    System.out.println(data);
&#125;
</code></pre>
<h2 id="查找"><a href="#查找" class="headerlink" title="查找"></a>查找</h2><pre><code>//三种遍历方法对应三种查找,传入目标值，返回节点对象

//前序查找
public TreeNode frontSearch(int target)&#123;
    //当temp不为空说明已经查到，没有继续的意义
    TreeNode temp = null;
    if(target==this.data)&#123;
        return this;
    &#125;else &#123;
        if(left!=null)&#123;
            temp =  left.frontSearch(target);
        &#125;
        if(temp !=null)&#123;
            return  temp;
        &#125;

        if(right!=null)&#123;
            temp= right.frontSearch(target);
        &#125;

    &#125;
    return temp;

&#125;

//中序查找
public TreeNode midSearch(int target)&#123;
    TreeNode temp = null;
    if(left!=null)&#123;
        temp = left.midSearch(target);
    &#125;
    if(temp!=null)&#123;
        return temp;
    &#125;

    if(this.data==target)&#123;
        return this;
    &#125;
    if(right!=null)&#123;
        temp = right.midSearch(target);
    &#125;
    return temp;

&#125;

//后序查找
public TreeNode afterSearch(int target)&#123;
    TreeNode temp = null;
    if (left!=null)&#123;
        temp = left.afterSearch(target);
    &#125;
    if(temp!=null)&#123;
        return temp;
    &#125;
    if (right!=null)&#123;
        temp = right.afterSearch(target);
    &#125;
    if(temp!=null)&#123;
        return temp;
    &#125;
    if(this.data==target)&#123;
        return this;
    &#125;
    return temp;
&#125;
</code></pre>
<h2 id="删除"><a href="#删除" class="headerlink" title="删除"></a>删除</h2><pre><code>//这是节点类中的方法

//删除节点，
public void delete(int target)&#123;
    //子节点满足条件就断掉
    if(left!=null&amp;&amp;left.data==target)&#123;
        left = null;
        return;
    &#125;
    if(right!=null&amp;&amp;right.data==target)&#123;
        right = null;
        return;
    &#125;
    //遍历除了叶子节点之外的所有节点
    if(left!=null)&#123;
        left.delete(target);
    &#125;
    if(right!=null)&#123;
        right.delete(target);
    &#125;

&#125;

//二叉树类的方法，调用节点类中的方法并处理空树和只有一个根节点的情况

//删除节点
public void delete(int target)&#123;
    //根节点没有双亲，无法删除，单独判断
    if(root==null)&#123;
        throw new RuntimeException(&quot;树空&quot;);
    &#125;else if(root.data==target)&#123;
        root = null;
    &#125;else &#123;
        //调用节点类方法
        root.delete(target);
    &#125;
&#125;
</code></pre>
<hr>
<p>更新自2&#x2F;24&#x2F;2022 10:14:10 PM<br>夜晚的风儿应该很温柔，而我呆在宿舍里无福消受。</p>
<hr>
<h1 id="二叉树顺序存储"><a href="#二叉树顺序存储" class="headerlink" title="二叉树顺序存储"></a>二叉树顺序存储</h1><pre><code>/*
1，index是下标

2，2*index+1表示左孩子，2*index+2表示右孩子

3，(n-1)2是双亲节点

4，顺序二叉树通常只考虑完全二叉树
*/

//只需要一个数组即可
class ArrayBinaryTree &#123;
    //存储树的数组
    int []array;
    //相当于传一棵树进来
    public ArrayBinaryTree(int []array)&#123;
        this.array = array;
    &#125;
&#125;
</code></pre>
<h2 id="遍历-1"><a href="#遍历-1" class="headerlink" title="遍历"></a>遍历</h2><pre><code>//前序遍历

public  void frontShow(int index)&#123;
    //空数组或是数组为0无法遍历
    if(array==null||array.length==0)&#123;
        throw new RuntimeException(&quot;无法遍历&quot;);
    &#125;
    //打印出当前值
    System.out.println(array[index]);
    //存在左节点就向左递归
    if(2*index+1&lt;array.length)&#123;
        frontShow(2*index+1);
    &#125;
    //存在右节点就向右递归
    if(2*index+2&lt;array.length)&#123;
        frontShow(2*index+2);
    &#125;
&#125;

//中序遍历
public void midShow(int index)&#123;
    if(array==null||array.length==0)&#123;
        throw new RuntimeException(&quot;无法遍历&quot;);
    &#125;
    if(index*2+1&lt;array.length)&#123;
        midShow(index*2+1);
    &#125;
    System.out.println(array[index]);
    if(index*2+2&lt;array.length)&#123;
        midShow(index*2+2);
    &#125;
&#125;

//后序遍历
public void afterShow(int index)&#123;
    if(array==null||array.length==0)&#123;
        throw new RuntimeException(&quot;无法遍历&quot;);
    &#125;

    if(2*index+1&lt;array.length)&#123;
        afterShow(2*index+1);
    &#125;
    if(2*index+2&lt;array.length)&#123;
        afterShow(2*index+2);
    &#125;
    System.out.println(array[index]);
&#125;
</code></pre>
<h2 id="查找-1"><a href="#查找-1" class="headerlink" title="查找"></a>查找</h2><pre><code>//遍历数组就行了
public int search(int target)&#123;
        //返回下标是-1时说明没找到
        int index = -1;
        for(int i = 0;i&lt;array.length;i++)&#123;
            if(array[i]==target)&#123;
                index = i;
            &#125;
        &#125;
        return index;

    &#125;
</code></pre>
<hr>
<p>今天的时间都被破烂选修课浪费了···</p>
<p>晚安，好梦（困到神志不清zzz）</p>
<p>更新于 2&#x2F;25&#x2F;2022 11:18:53 PM </p>
<hr>
<h1 id="线索二叉树"><a href="#线索二叉树" class="headerlink" title="线索二叉树"></a>线索二叉树</h1><p>有些节点指针域为空，浪费了空间</p>
<pre><code>//线索化二叉树，中序
public void threadTree(TreeNode node)&#123;
    //递归往最左边找，找到叶子节点的下一个结束
    if(node==null)&#123;
        return;
    &#125;
    //递归左子树
    threadTree(node.left);
    //左子树为空指向前驱节点
    if(node.left==null)&#123;
        node.left = pre;
        //leftType==1证明他的左子树指向前驱节点
        node.leftType = 1;
    &#125;
    if(pre!=null&amp;&amp;pre.right ==null)&#123;
        pre.right = node;
        //rightType == 1证明右子树指向后继节点
        pre.rightType = 1;
    &#125;

    //left向递归遍历顺序后移一个
    pre = node;
    //递归右子树
    threadTree(node.right);
&#125;

//线索二叉树中序遍历
public void threadShow()&#123;
    TreeNode node = root;
    while (node!=null)&#123;
        while(node.leftType==0)&#123;
            node = node.left;
        &#125;
        System.out.println(node.data);
        while(node.rightType==1)&#123;
            node = node.right;
            System.out.println(node.data);
        &#125;
        node = node.right;
    &#125;


&#125;
</code></pre>
<hr>
<p>又慌又累又无聊，什么时候出去走走吧······<br>2&#x2F;26&#x2F;2022 10:05:17 PM </p>
<hr>
<h1 id="哈夫曼树"><a href="#哈夫曼树" class="headerlink" title="哈夫曼树"></a>哈夫曼树</h1><pre><code>public static Node hfm(int [] row)&#123;
        List&lt;Node&gt; list = new ArrayList&lt;&gt;();
        for (int i : row) &#123;
            list.add(new Node(i));
        &#125;
        //当列表中只剩下目标树的根节点时停止循环
        while(list.size()&gt;1)&#123;
            //排序，需要list中的元素实现Comparable接口
            Collections.sort(list);
            //把权值最小的两个取出当第一棵子树的左右孩子节点
            Node leftNode = list.get(0);
            Node rightNode = list.get(1);
            //双亲节点的权值是两孩子节点的权值和
            Node parent = new Node(leftNode.value+rightNode.value);
            //把孩子节点和双亲节点连起来
            parent.left = leftNode;
            parent.right = rightNode;
            //移除列表中的孩子节点
            list.remove(leftNode);
            list.remove(rightNode);
            //把双亲节点加入
            list.add(parent);
        &#125;
        //返回哈夫曼树的根节点
       return list.get(0);
    &#125;
</code></pre>
]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
  </entry>
  <entry>
    <title>多线程与并发</title>
    <url>/2022/03/02/%E5%A4%9A%E7%BA%BF%E7%A8%8B%E4%B8%8E%E5%B9%B6%E5%8F%91/</url>
    <content><![CDATA[]]></content>
  </entry>
  <entry>
    <title>字符串</title>
    <url>/2022/02/26/%E5%AD%97%E7%AC%A6%E4%B8%B2/</url>
    <content><![CDATA[<h1 id="字符串"><a href="#字符串" class="headerlink" title="字符串"></a>字符串</h1><h2 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h2><p>多线程时用StringBuffer,单线程用StringBiulder,不需要可变用String</p>
<p>效率：SBiulder&gt;SBuffer&gt;String<br>##String类</p>
<span id="more"></span>
<pre><code>//未初始化会报错
        // String str;
        //System.out.println(str);

// 值为null时可以正常打印
        //str = null;
        //System.out.println(str);


//但是调用 值为null 的字符串会空指针异常
        // System.out.println(str.length());

//创建方法
        // 推荐，将常量池中的“aaaa”的引用赋给str
        String str = &quot;aaaa&quot;;
        //不推荐，实际上创建了两个String对象，浪费空间。
        String str1 = new String(&quot;aaaa&quot;);

        char[]arr1 =&#123;&#39;1&#39;,&#39;2&#39;,&#39;3&#39;,&#39;4&#39;&#125;;
        String str2 = new String(arr1,2/*开始下标*/,2/*切片元素数量*/);

        str2 = String.valueOf(arr1,2,2);

        System.out.println(str2);//34

//用String类中的静态方法String.valueOf(ElementType)将基本数据类型转换为字符串
        //System.out.println(String.valueOf(5));
        //System.out.println(String.valueOf(5.001));
        //System.out.println(String.valueOf(str+&quot; &quot;+str1));//转换的是一个对象时，会调用对象的toString方法

//强制类型转换不可行
        //int i = 1;
        //System.out.println((String) i);

//拼接字符串,会把括号里的全部变成字符串类型
        //System.out.println(&quot;111&quot;+&quot;222&quot;+10);

//查找方法
        //charAt(index)返回元素
        //indexOf(ch/String)返回第一次出现下标
        //lastIndexOf(ch/String)反向开始搜索，返回最后一次出现下标
</code></pre>
<hr>
<p>待续···</p>
<hr>
<h2 id="StringBuilder和StringBuffer"><a href="#StringBuilder和StringBuffer" class="headerlink" title="StringBuilder和StringBuffer"></a>StringBuilder和StringBuffer</h2><p>长度都是可变的</p>
<p>StringBuilder:非线程安全的</p>
<p>StringBuffer:线程安全的</p>
<h3 id="API"><a href="#API" class="headerlink" title="API"></a>API</h3><pre><code>//创建
new ...();
new ...(int 长度--默认16);
new ...(String);
new ...(char 序列--暂时粗略理解为字符串吧);

//添加到字符串最后
append(基本数据类型或者是char数组);

//插入
insert(下标，基本数据类型或字符数组);

//删除[int ,int)内的所有字符，如果两值一样，不会删除任何元素
delete(int,int);

//返回元素
charAt(int index)

//返回当前容量(并非字符数量)，默认16，超过时自动扩容
capacity(实例对象)；

//替换[start,end)
replace(int start,int end,String);

//反转
reverse();
</code></pre>
<hr>
<h2 id="更新于-2-x2F-26-x2F-2022-5-35-28-PM"><a href="#更新于-2-x2F-26-x2F-2022-5-35-28-PM" class="headerlink" title="更新于 2&#x2F;26&#x2F;2022 5:35:28 PM "></a>更新于 2&#x2F;26&#x2F;2022 5:35:28 PM </h2>]]></content>
      <categories>
        <category>JAVASE</category>
      </categories>
  </entry>
  <entry>
    <title>数据库</title>
    <url>/2022/02/28/%E6%95%B0%E6%8D%AE%E5%BA%93/</url>
    <content><![CDATA[<h1 id="mysql"><a href="#mysql" class="headerlink" title="mysql"></a>mysql</h1><p>在windows下不区分大小写</p>
<p>mysql -uroot -p;登录</p>
<p>show databases;看数据库</p>
<p>use ····；转到某数据库</p>
<p>show tables;</p>
<p>net start sql;使用</p>
<p>net stop sql;停用</p>
<p>exit，\q，quit;退出数据库</p>
<p>contal + c;结束本次语句输入</p>
<h2 id="数据查询语言DQL"><a href="#数据查询语言DQL" class="headerlink" title="数据查询语言DQL"></a>数据查询语言DQL</h2><p>代表关键字：select</p>
<h3 id="单表查询"><a href="#单表查询" class="headerlink" title="单表查询"></a>单表查询</h3><p>selct ··· 5</p>
<p>from ···1</p>
<p>where ···2</p>
<p>group by···3</p>
<p>having ···4</p>
<p>order by···6</p>
<h4 id="操作符：用于条件语句where下"><a href="#操作符：用于条件语句where下" class="headerlink" title="操作符：用于条件语句where下"></a>操作符：用于条件语句where下</h4><p>&lt;&gt;或者!&#x3D; (不等于)</p>
<p>&lt; &gt; &#x3D; &gt;&#x3D; &lt;&#x3D;</p>
<p>between ···and ···· []闭区间</p>
<p>is null    null不能用等号，专门用 is null,is not null判断</p>
<p>and or </p>
<p>in         where ···in(···，···，···)；判断在不在集合内,相当于or的简洁写法。</p>
<p>not 一般是 not in ,is not null</p>
<p>like %代表多个字符，_代表一个字符,如果要使用原字符，前面加转移符号 \</p>
<p>like’ ‘</p>
<p>####表达式的优先级<br>and&gt;or······不用管，用括号即可</p>
<p>###排序<br>order by···，····<br>升序 order by···asc（默认）<br>降序 ···········desc</p>
]]></content>
      <categories>
        <category>数据库</category>
      </categories>
  </entry>
  <entry>
    <title>无题</title>
    <url>/2022/02/26/%E6%97%A0%E9%A2%98/</url>
    <content><![CDATA[<h2 id="几天前的雪了，那时候都没好好看过它们···有些东西没有了才会觉得重要······它们看起来就很安静，不是吗？"><a href="#几天前的雪了，那时候都没好好看过它们···有些东西没有了才会觉得重要······它们看起来就很安静，不是吗？" class="headerlink" title="几天前的雪了，那时候都没好好看过它们···有些东西没有了才会觉得重要······它们看起来就很安静，不是吗？"></a>几天前的雪了，那时候都没好好看过它们<span id="more"></span>···有些东西没有了才会觉得重要······它们看起来就很安静，不是吗？</h2><p><img src="/myimage/2022.2.26_1.png" alt="avatar"><br><img src="/myimage/2022.2.26_2.png" alt="avatar"></p>
<hr>
<h2 id="2-x2F-26-x2F-2022-10-30-24-PM"><a href="#2-x2F-26-x2F-2022-10-30-24-PM" class="headerlink" title="2&#x2F;26&#x2F;2022 10:30:24 PM "></a>2&#x2F;26&#x2F;2022 10:30:24 PM </h2><hr>
<hr>
<hr>
<hr>
<p>状态太差，效率太低，是时候改变学习方式了。<br>继续······又过了无聊的一天，如果今天是我生命的最后一天，我一定会做很多有意思的事，也会更加勇敢吧···<br>可惜，今天不是。</p>
<p>晚安，好梦，不，无梦········································································································································································································································</p>
<hr>
<h2 id="2-x2F-27-x2F-2022-10-10-48-PM"><a href="#2-x2F-27-x2F-2022-10-10-48-PM" class="headerlink" title="2&#x2F;27&#x2F;2022 10:10:48 PM "></a>2&#x2F;27&#x2F;2022 10:10:48 PM </h2>]]></content>
      <categories>
        <category>日志</category>
      </categories>
  </entry>
  <entry>
    <title>栈篇</title>
    <url>/2022/02/23/%E6%A0%88/</url>
    <content><![CDATA[<p>栈是一种后进先出结构(FILO)</p>
<h1 id="用数组实现栈"><a href="#用数组实现栈" class="headerlink" title="用数组实现栈"></a>用数组实现栈</h1><span id="more"></span>
<pre><code>任何实现入栈出栈时间复杂度都是O(1)
class Stack&#123;
private int maxSize;
    private int []datas ;
    private int top = -1;//栈顶初始值为-1
    public Stack(int maxSize)&#123;
        this.maxSize = maxSize;
        datas = new int[maxSize];
    &#125;

//入栈
public void push(int data)&#123;
    if(isFull())&#123;
        throw new RuntimeException(&quot;栈满了无法加入&quot;);
    &#125;
    top++;//栈顶先移动
    datas[top] = data;
&#125;

//出栈
public int pop()&#123;
    if(isEmpty()) &#123;
        throw new RuntimeException(&quot;栈空无法出栈&quot;);
    &#125;
    int temp = datas[top];
    top--;
    return temp;
&#125;
</code></pre>
<h1 id="用链表实现"><a href="#用链表实现" class="headerlink" title="用链表实现"></a>用链表实现</h1><pre><code>//链表实现不用担心栈满的问题
class LinkedStack &#123;
Node head = new Node();
Node top;
&#125;

//入栈，头节点为栈底，先连接头节点，在把top指向入栈的
public void push(Node node)&#123;
    node.next = top;
    top = node;
&#125;

//出栈
public int  pop()&#123;
    if(isEmpty())&#123;
        throw new RuntimeException(&quot;栈空无法出栈&quot;);
    &#125;
    int a = top.data;
    top = top.next;
    return a;
&#125;
</code></pre>
<h1 id="共享栈"><a href="#共享栈" class="headerlink" title="共享栈"></a>共享栈</h1><pre><code>//特定环境使用，一个数组中放两个栈，左边初始top1 = -1，右边初始top2 = maxSize.
//两个栈应该类型相同且一个栈增加的同时另一个栈相应减小，否则很快也会满

class ShareStack &#123;
    private int maxSize;
    private int[]datas;
    private int top1;
    private int top2;
&#125;

//判断栈满,top1,top2相邻
public boolean isFull()&#123;
    return top1+1==top2;
&#125;

//判断栈空，分别判断
public boolean isEmpty(int number)&#123;
    if(number==1)&#123;
        return top1==-1;
    &#125;else return top2==maxSize;
&#125;


//进栈，需要指定哪一个栈
public void push(int data,int number)&#123;
    //满了抛出异常
    if(isFull())&#123;
        throw new RuntimeException(&quot;栈满无法加入&quot;);
    &#125;
    //当number==1时表示加到左边的栈，否则加到右边的栈
    if(number==1)&#123;
        top1++;
        datas[top1] = data;
    &#125;else &#123;
        top2--;
        datas[top2] = data;
    &#125;
&#125;

//出栈，需要指定哪一个栈
public int pop(int number)&#123;
    //判断是否为空
    if(isEmpty(number))&#123;
        throw new RuntimeException(&quot;栈空无法出栈&quot;);
    &#125;

    if(number==1)&#123;
        int l = datas[top1];
        top1--;
        return l;
    &#125;else &#123;
        int r = datas[top2];
        top2--;
        return r;
    &#125;
&#125;
</code></pre>
]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
  </entry>
  <entry>
    <title>链表篇</title>
    <url>/2022/02/23/%E9%93%BE%E8%A1%A8/</url>
    <content><![CDATA[<h1 id="链表和顺序表的优劣"><a href="#链表和顺序表的优劣" class="headerlink" title="链表和顺序表的优劣"></a>链表和顺序表的优劣</h1><span id="more"></span>
<p>1,因为有数组下标的存在，顺序表在查找方面领先链表，时间复杂度为O(1),而链表查找需要遍历，因此时间复杂度为o(N)。</p>
<p>2，增加或删除元素方面，链表优势，顺序表每次插入或者删除都需要移动大量元素O(N),链表需要查找到增加或删除的位置(通过指针)，也是O(N),但是插入的越频繁链表的优势体现的越大(指针移动的距离变少)。</p>
<h1 id="双向链表"><a href="#双向链表" class="headerlink" title="双向链表"></a>双向链表</h1><p>1,除了data和next外，还有个指向前驱节点的prior指针。</p>
<p>2,空间占用更多。用空间换时间效率。</p>
<pre><code>插入操作：//找到插入位置的前一个位置temp
        node.next = temp.next;//设置待插入节点的next
        node.prior = temp;//设置待插入节点的prior
        temp.next.prior = node;//将待插入节点的下一个节点的prior指向待插入节点
        temp.next = node;//将temp指向待插入节点

删除操作：//找到待删除的节点temp
        //直接跳过删除节点
        temp.prior.next = temp.next
        temp.next.prior = temp.prior;
        
    
</code></pre>
]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
  </entry>
  <entry>
    <title>队列</title>
    <url>/2022/02/24/%E9%98%9F%E5%88%97/</url>
    <content><![CDATA[<p>先进先出结构</p>
<h1 id="用数组实现"><a href="#用数组实现" class="headerlink" title="用数组实现"></a>用数组实现</h1><h2 id="普通"><a href="#普通" class="headerlink" title="普通"></a>普通</h2><span id="more"></span>
<p>&#x2F;&#x2F;每次出队列都需要移动所有的元素，解决方法，循环队列。</p>
<h2 id="循环队列"><a href="#循环队列" class="headerlink" title="循环队列"></a>循环队列</h2><p>&#x2F;&#x2F;解决了移动元素的问题，用头尾指针的移动替代了。</p>
<pre><code>//成员变量
class Queue &#123;
    private int maxSize;
    private int []datas;
    /*
    front 指向队列头
    rear 指向队列尾的后一个元素
     */
    int front;
    int rear;
&#125;

//判断队列是否空，头和尾重合即为空
public boolean isEmpty()&#123;
    return front==rear;
&#125;
//判断队列是不是满，尾指针一直指向空
public boolean isFull()&#123;
    return (rear+1)%maxSize==front;
&#125;

//入队列操作
public void enter(int data)&#123;
    if(isFull())&#123;
        throw new RuntimeException(&quot;队列满了无法加入&quot;);
    &#125;
    datas[rear] = data;//值付给队尾指针
    rear = (rear+1)%maxSize;//队尾加一，超过循环后取余maxSize
&#125;

//出队列操作
public int  delete()&#123;
    if(isEmpty())&#123;
        throw new RuntimeException(&quot;队列空无法出队&quot;);
    &#125;
    int num = datas[front];
    front = (front+1)%maxSize;
    return num;
&#125;

/*如果头在尾巴的前面，长度就是尾减去头，
  如果头在尾后面，长度为max-front这一段加上rear-0这一段
*/
//长度
public int length()&#123;
    return (maxSize+rear-front)%maxSize;
&#125;
</code></pre>
<h1 id="用链表实现"><a href="#用链表实现" class="headerlink" title="用链表实现"></a>用链表实现</h1><p>&#x2F;&#x2F;不用考虑溢出问题</p>
<pre><code>//成员变量，需要一个头节点
public class LinkQueue &#123;
    Node front;
    Node rear;
    Node head;
&#125;

//添加节点，先连接上下一个节点，在移动rear指针。
public void enter(Node node)&#123;
    rear.next = node;
    rear = rear.next;
&#125;

//删除节点
public int delete()&#123;
    if(isEmpty())&#123;//头和尾重合即为空
        throw new RuntimeException(&quot;队列空无法出队列&quot;);
    &#125;
    int a = front.next.data;

    //如果要删除的节点是最后一个，为了不让rear指针指空，把rear放回头节点
    if(front.next == rear)&#123;
        rear = head;
    &#125;
    //跨过头结点的下一个节点即可
    front.next = front.next.next;

    return a;
&#125;
</code></pre>
]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
  </entry>
</search>
